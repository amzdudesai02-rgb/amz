# Nginx Configuration for Hostinger VPS
# Place this in /etc/nginx/sites-available/amzdudes-backend

server {
    listen 80;
    server_name api.amzdudes.io;  # Change to your domain

    # Logging
    access_log /var/log/nginx/amzdudes-backend-access.log;
    error_log /var/log/nginx/amzdudes-backend-error.log;

    # Increase timeouts for API calls
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        # WebSocket support (if needed)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Health check endpoint
    location /api/health {
        proxy_pass http://127.0.0.1:8000/api/health;
        access_log off;
    }

    # Static files (if you add any)
    location /static {
        alias /var/www/amzdudes-backend/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Redirect HTTP to HTTPS (after SSL setup)
# server {
#     listen 80;
#     server_name api.amzdudes.io;
#     return 301 https://$server_name$request_uri;
# }

# HTTPS configuration (after SSL setup)
# server {
#     listen 443 ssl http2;
#     server_name api.amzdudes.io;
#     
#     ssl_certificate /etc/letsencrypt/live/api.amzdudes.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.amzdudes.io/privkey.pem;
#     
#     # SSL configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # Same location blocks as above
# }

